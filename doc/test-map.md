# What behavior is verified?

| Behavior | Public entrypoint | Test | Remaining check |
| --- | --- | --- | --- |
| Parse a completed Codex reply | `CodexEventParser.parse` | `parsesCompletedCodexResponse` | Real CLI smoke passed 2026-09-04 |
| Ignore malformed/non-message events | `CodexEventParser.parse` | `ignoresMalformedAndNonMessageEvents` | None |
| Remove markup before speech | `SpokenText.clean` | `cleansTextForSpeech` | Listen to an actual response |
| Start then resume one conversation without duplicating durable instructions or activating a failed/discarded routed context | `CodexClient.ask` | `startsAndResumesConversationThroughPublicClient` verifies `exec resume`, one compact dynamic envelope per request, and no repeated default prompt; `failedLaunchDoesNotActivateAProposedProjectContext` and `clearContextWinsTheThreadPersistenceRace` cover transactional selection and cancellation | Initial and resumed real CLI smoke passed; v3 starts in the portable workspace |
| Queue speech, dropped files, and App-Intent messages through route/start, steer an active task without rerouting, then resume FIFO after response/speech | `ConversationInputQueue` → `CodexClient.steer` | `ConversationInputQueueTests`, `steersRunningConversationThroughPublicClient` | Speak and drop repeatedly across one real long task |
| Persist every accepted message before routing, recover FIFO after restart, and remove it only after acknowledgement | `ConversationInputJournal` → `ConversationInputQueue` | `ConversationInputJournalTests`, delivery-identity queue test | Terminate an installed app during route and confirm automatic recovery |
| Reject whitespace messages, zero-byte files, and invalid persisted queue entries before any Codex request | menu/App Intent/drop/journal/queue → `CodexClient` | queue empty-input regression, journal structural validation, `StatusFileDropTests`, `rejectsEmptyPromptBeforeLaunchingCodex` | Drop a Finder zero-byte file and submit an empty App Intent |
| Keep exactly one menu-bar process across installed and build copies | `SingleInstanceLock` → app entrypoint | `SingleInstanceLockTests` verifies cross-process exclusion and reuse | Launch both app bundles and confirm one PID/icon |
| Discover Codex, migrate old block markers to warning-only behavior, and classify operational failures | `CodexExecutableLocator` / `CodexClient.ask` | `discoversCodexWithoutAPackageManagerPath`, `migratesAnOldBlockedCandidateToWarningOnly`, `rejectsMissingExecutable`, `classifiesOperationalFailures` | Expire a real login and verify recovery text |
| Install or update only the latest Homebrew Codex Cask, with signature and capability checks as warnings | `CodexMaintenance.maintain` | `installsOnlyTheCodexCaskWhenCodexIsMissing`, `updatesOnlyAHomebrewManagedCodex`, `retainsTheInstalledCaskWhenAnUpgradeFails`, `activatesAnUntrustedUpdateWithAWarning`, `activatesLatestEvenWhenARequiredCapabilityIsMissing`, `adoptsLatestHomebrewCodexWithoutDeletingAnExistingExecutable`, `checksForUpdatesWeekly`, `clearsAResolvedLoginWarningWithinTheWeeklyWindow`, `reportsMissingHomebrewWithoutTryingAnotherInstaller` | Let an installed app perform one natural background upgrade |
| Show the persisted installed Codex version | `CodexMaintenance.displayVersion` → menu | `formatsThePersistedVersionForTheMenu`, `discoversTheVersionWithoutHomebrewOnFirstCheck` | Inspect the installed menu after maintenance |
| Run Codex maintenance only outside listening, work, and speech | `AssistantState.allowsCodexMaintenance` | `permitsCodexMaintenanceOnlyWhileInactive` | Observe the installed app across its scheduled maintenance boundary |
| Expose useful visual state through an always-available native composed menu-bar icon | `AssistantState` / `StatusIconAnimator` | `reportsAssistantStates`, `composesNativeStatusVisuals` | Inspect the installed motion, meeting dot, and warning mark |
| Hide redundant idle status while keeping active and failed status visible | `AssistantState.showsStatusInMenu` → menu header | `keepsActiveStatesVisible` | Open the installed idle and active menus |
| Convert a recorded physical left/right modifier into one hold-to-talk cycle, with cancellation preserving the prior choice | `ShortcutRecorder` / `AssistantShortcutStore` → `GlobalHotKey` | `AssistantShortcutTests`, `tracksFunctionKeyEdgesOnlyOnce`, `acceptsOnlyThePhysicalFunctionKey` | Record and cancel each physical modifier in the installed menu |
| Route Fn chords without submitting dictation | `GlobalHotKey` → delayed listen / `AppDelegate.handleChord` | `recognizesOnlyMnemonicFnShortcuts` | Physical Fn+R/P/Escape with permission |
| Ignore empty/no-speech recordings | `SpeechController` and `CodexClient.ask` | `suppressesNoSpeechErrors`, `rejectsEmptyPromptBeforeLaunchingCodex` | Physical silent Fn cycle |
| Keep the everyday menu compact and stable, with flat permission groups, flat timing rows plus cold storage/version metrics under `More…`, source count in `Sources (N)`, and Clear Context under Options | Menu-bar UI | Manual | Verify short labels, separators, and stable row positions |
| Persist remappable hold-key presets and runtime Codex access profiles without hard-coded model choices | `AssistantShortcutStore` / `AssistantAccessProfile` | `AssistantShortcutTests`, `AssistantAccessProfileTests` | Select each available menu profile and inspect one real child process |
| Parse language-neutral app controls without matching ordinary speech | `AssistantControlCommand.request` | `parsesLanguageNeutralAssistantControls` | Ask naturally to repeat, open Usage, open a diagram, and show a result |
| Validate browser diagram handoff before consent | `DiagramEditor.request` | `validatesDiagramEditorRequests` covers valid Unicode, malformed/unrelated XML, symlink, and size limit | Confirm/cancel the native sheet and simulate browser-open failure manually |
| Speak through the current macOS system voice and output, including a runtime route change | `SpeechOutput.speak` | `recoversSpeechOutputAfterDefaultRouteChange` verifies recreation, pause preservation, late-callback isolation, and FIFO continuation; real system diagnostic selected `com.apple.siri.natural.de-DE-C` | Switch between speakers and headphones during one installed answer |
| Allow user-requested local file work without a path allowlist | `CodexClient.ask` | Fake CLI verifies initial and resumed danger-full-access mode and absence of `--add-dir` | Request a change outside the assistant workspace |
| Disable only optional connectors and hooks advertised by the installed Codex while preserving the main request on probe failure | `CodexFeatureIsolation` → `CodexClient.ask` | `CodexFeatureIsolationTests`; fake CLI verifies discovered isolation flags | None |
| Keep ordinary status exclusively in the menu bar and preserve a transient file-drop target while crossing from icon to panel | `AppDelegate.render` / `StatusFileDropController` | `StatusFileDropTests` | Drag files over the installed icon and into the panel |
| Attach a one-shot screenshot only for explicit screen references | `ScreenContextPolicy` → `CodexClient.ask` | `capturesOnlyExplicitScreenReferences`, client image test | First macOS permission prompt |
| Keep durable memory in its workspace while allowing task files elsewhere | `CodexClient.ask` and assistant rules | Workspace bootstrap and unrestricted-mode client tests | Ask it to remember a preference |
| Discover models and route turns semantically with a live text-only planner, retain the active project context while temporarily using the discovered default after a slow routing pass, and bound router subprocess output | Codex app-server catalog → isolated routing fork | `discoversAndSelectsModelsSemantically`, `backsOffAfterSlowRouting`, `preservesActiveContextDuringRoutingCooldown`, `boundsRouterProcessOutput`, `fallsBackToTheLiveCodexDefaultWhenPlanningFails` | Measure installed routing after catalog changes and timeout fallback |
| Show distinct, non-blinking pipeline states and warning overlay | `StatusAnimation.motion` / `StatusIconAnimator` | `animatesOnlyActiveStates` | Inspect symbols, warning badge, gentle movement, and Reduce Motion |
| Speak ordered user-facing commentary without private reasoning | live Codex events → one `SpeechOutput` queue | `reportsSafeCodexProgress` | Listen through a multi-step real request |
| Clear rolling context without deleting durable memory | `CodexClient.clearContext` | `clearsContextWithoutDeletingWorkspaceData` | Use menu item, then ask a follow-up |
| Separate durable project contexts and expire only inactive mappings | `ProjectContextStore` / semantic `ModelRouter` | `migratesTheExistingConversationIntoGeneralContext`, `keepsIndependentProjectThreads`, `expiresOnlyTheProjectMapping`, `rejectsInvalidModelSelectedKeys`, `discoversAndSelectsModelsSemantically` | Move naturally between two named projects and confirm continuity |
| Compact a heavily used context once after ten quiet minutes without blocking new speech | `CodexContextMaintenance.compactIfNeeded` | threshold, nonblocking activity snapshot, explicit cancellation, isolated app-server arguments/environment, success, JSON-RPC failure, and timeout tests | Observe queued speech through one natural compaction above 75% |
| Cancel work before or after process launch without persisting a proposed context | `CodexClient.cancel` | `cancellationBeforeLaunchNeverStartsCodex`, `cancellationAfterLaunchReturnsCancelled`, cancellation-before/during-login tests in `CodexContextMaintenanceTests` | None |
| Retain and safely recover cleared task ownership for complete deletion | `CodexClient.clearContext` / `deleteStoredThreads` | `clearedContextRemainsOwnedForDeletion`, `discoversPreRegistryWorkspaceTasksForDeletion`, `discoversOnlyVerifiedLegacyWorkspaceTasks`, `deletesOnlyStoredAssistantThreads` | Two-click installed deletion intentionally not executed |
| Show cached context, active workers, diff, storage, weekly allowance, reset credits, last-call timings, and one flat recent-source list | metrics/source stores → main menu | `loadsCachedAssistantMetrics`, `CodexAccountMetricsTests`, `RecentSourcesTests`, progress/diff parser tests | Inspect and click installed menu rows |
| Bound session-history work regardless of accumulated Codex history | `AssistantMetricsLoader.load` | `boundsSessionMetricsReads` verifies a large prefix is never loaded and the newest complete event remains visible | Installed idle footprint measured at about 95 MB with 2.9 GB of existing session history |
| Open web usage from the weekly row | `AppDelegate.openUsage` | Installed URL inspection | Click row and confirm signed-in page |
| Prefer Codex Desktop, then visible Terminal resume, then local transcript behind one stable `Open Chat` action without duplicate launches | `CodexChatLauncher.open` / `ChatOpenGate` | `fallsBackFromIncompatiblePaginatedChats`, `opensTerminalWhenDesktopChatCannotBeOpened`, `preventsDuplicateChatWindows` | Click with an incompatible Desktop and approve the first Automation prompt |
| Publish discoverable native actions that share Aven's existing control and conversation paths | `AvenControlIntent` / `SendToAvenIntent` | Xcode build verifies `Metadata.appintents` generation | Run each action once from Shortcuts |
| Accept typed menu input through the same ordered conversation path as speech and dropped files; retain it across failure/quit/context changes and snapshot attachments privately | `MenuTextInputView` → `ConversationInputJournal` → `ConversationInputQueue` | `StatusFileDropTests`, `ConversationInputJournalTests`, `ConversationInputQueueTests` cover rejection, FIFO recovery, immutable attachment snapshots, acknowledgement cleanup, and retry preservation | Type during routing, an active task, response, and speech |
| Read selected or copied text only on demand through a bounded active-task broker | `AssistantContextCommand` | `parsesAndBoundsOnDemandContext` covers fixed operations, rejects watch mode, and preserves bounded UTF-8 | Grant Accessibility, select text in another app, and ask about it |
| Expire recipes without executing them | `RecipeMaintenance.maintain` | `expiresAndDeletesTaskRecipes`, `preservesUnsafeOrMalformedTaskRecipes` | Observe one natural 90-day expiry |
| Follow the macOS system speech language | `SystemSpeechLanguage` → speech recognition and Codex request instruction | `resolvesTheSystemSpeechLanguage` | Change the system speech language and ask again |
| Maintain open-ended searchable memory and scoped preferences | `database/assistant.sqlite3` | Generic category, FTS, lifecycle, assistant trait, scoped rule, identity, style, and retention tests | Record and later invalidate a real locator |
| Discover and incrementally install every valid bundled skill into native `.agents/skills` without a Swift registry, while preserving user skills and memory | `WorkspaceBootstrap.prepare` | `WorkspaceBootstrapTests`; `scripts/validate_skills.py` validates every bundled skill | First launch on a second macOS account |
| Stream consented meeting audio into a private live on-device transcript while questions remain usable and expose a persistent click-to-stop control | `AppDelegate.toggleMeetingRecording` → `MeetingStartGate` → `MeetingRecorder` | `MeetingRecorderTests` verifies serialized/cancellable permission start, durable JSONL, metadata, and absence of raw audio; control parser tests cover start/stop | Approve the native permissions, record a short real call, ask one live question, and click Stop |
| Gate OpenAI forwarding, local capabilities, and web research | `AIForwardingConsent`, `PermissionController`, calendar broker, and Codex boundary | `persistsOnlyCurrentForwardingConsent`, `requiresForwardingConsentAtTheProcessBoundary`; fake CLI verifies `--search` | Accept/revoke disclosure, toggle each permission, and run a real calendar search |
| Request core macOS permissions on first conversation use | `AppDelegate.startListening` → `SpeechController.start` / `PermissionController.prepareForConversation` | Authorization sequencing is exercised through the real controller path | Reset TCC entries, click Listen, and approve each native sheet |
| Keep secrets outside readable metadata and replace by stable identity | `CredentialVault.store` / `resolve` | `storesSecretsOutsideReadableMetadata` | Real Keychain add/read/delete smoke passed 2026-09-04 |
| Refresh source-backed credentials without printing values | `CredentialVault.refresh` | `refreshesAChangedEnvironmentSource` | Refresh a real `.env.local` key after unlock |
| Expire browser sessions and enforce maximum TTL | `CredentialVault.prune` | `expiresBrowserSessionsAndRejectsLongTTLs` | Observe one natural expiry |
| Reject malformed metadata and symlink credential sources | `CredentialVault.resolve` / `envValue` | `rejectsMalformedMetadataAndSymbolicLinkSources` | None |
| Authenticate metadata against its current Keychain-bound digest and reject edited or replayed authority fields | `CredentialVault.list` / `resolve` / `refresh` | `rejectsTamperedAndReplayedMetadata`, `migratesOnlyLegacyMetadataWithoutAuthorityFields` | Modify a copied installed-app record while Keychain remains intact and confirm fail-closed behavior |
| Fail closed rather than display unverified metadata while secret storage is locked | Keychain store boundary and vault command | `failsClosedWhenIntegrityStorageIsLocked` | Lock during ordinary work, then try vault listing and a credential action |
| Delete assistant credentials, files, and defaults together | `AssistantDataController.eraseAll` | `erasesOnlyTheAssistantCredentialCollection`, `erasesAssistantDataAndDefaultsTogether` | Two-click installed menu action intentionally not executed |
| Bound credential-backed child execution and inherited output pipes | `VaultCommand.run` | `terminatesTheEntireCredentialProcessGroup` plus bounded collector wait | Exercise a naturally timing-out authenticated tool |
| Restrict standing credential consent to one reviewed command | `CredentialVault.accessScope` / `validateAccess` | `bindsCredentialsToAnExactReviewedCommand` | Re-import one existing credential with a dedicated connector |
| Issue and revoke a vault/calendar/selection/clipboard capability bound to one Codex task subtree for its full process lifetime | `CodexClient.ask` → `TaskCapabilityBroker` → privileged local commands | `scopesCapabilitiesToOneCodexTask`, `authorizesPrivilegedCommandsOnlyForActiveTasks`, `keepsCapabilitiesValidForTheirExactTaskLifetime`, `bindsCapabilityToOneSpawnedTaskSubtree`, `VaultHelperTests` | Exercise a real credential-backed request, then replay its token after completion |
| Package a complete Retina application icon | `build.sh` → `AppIcon.icns` | Build and code-sign verification | Inspect Finder/Dock icon cache |
| Build calendar-versioned universal ZIP and DMG releases through the shared YunaBraska workflow | `scripts/check.sh` / `scripts/package.sh` → GitHub Actions | Local package smoke, architecture, plist, signature, DMG, and checksum verification | Run the manual release workflow with Developer ID and notarization secrets |
| Migrate early private storage and archive the verified home workspace under Aven without splitting or deleting data | `AssistantStorageMigration` → app entrypoint | `WorkspaceBootstrapTests` covers move, preservation, home-folder archival, cleanup, and conflict | Upgrade one installed early build with real stored context |
| Replace an installed app only after identity, signature, architecture, and App-Intent checks, restoring the prior bundle on ordinary failure or handled interruption | `install.sh` | Shell syntax, ShellCheck, two successful real upgrades, independent installer review | Automated signal-injection harness is not yet present |
